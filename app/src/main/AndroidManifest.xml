<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="idv.ron.oogame_poke">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/loveicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/loveicon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".controller.Login"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity
            android:name=".controller.MainActivity"
            android:screenOrientation="portrait" >
        </activity>

        <activity android:name=".controller.heartHuntActivity" />
        <activity android:name=".controller.ChasemanActivity" />
        <activity android:name=".controller.ManHuntActivity" />

    </application>

</manifest>